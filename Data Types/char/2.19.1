#include <iostream.h>

int main(){
       int n1 = 100,n2;
       short int s1 = 2,s2;
       char c1 = 'A',c2;
       unsigned char u1 = 'U',u2;
       n2 = s1;
       s2 = n1 + c1;
       c2 = u1;
       u2 = n1;
       cout << n1 << " " << n2 ;
       cout << s1 << " " << s2 ;
       cout << c1 << " " << c2 ;
       cout << (int)c1 << " " << int(c2);
       cout << u1 << " " ;
       cout << (int)u1 << " " << (int)u2;

    return 0;
}
